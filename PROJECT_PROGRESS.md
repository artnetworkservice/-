# ความคืบหน้าโครงการ

## ปรับหน้าเรื่องและการจัดหน้า — 17 ก.ย. 2026

- ช่อง `เรื่อง` แสดงคำนำคงที่ `ราคากลางจัดซื้อพัสดุ` และรับข้อความต่อท้าย แสดงผลใน Word เป็นสองส่วนคั่นด้วยช่องว่างหนึ่งตำแหน่ง
- ย่อหน้ารายการ คำนำรายการ และสรุปท้ายใช้ `Thai Distributed` ตามต้นฉบับ แยกบรรทัดที่มีการขึ้นบรรทัดใหม่เป็นย่อหน้าต่อเนื่องเพื่อไม่ให้กระจายตัวอักษรของบรรทัดสั้น
- สรุปท้ายต่อด้วย `ในการจัดซื้อพัสดุ <เรื่องต่อท้าย> จำนวน <จำนวนรายการ> รายการ รวมเป็นเงินทั้งสิ้น <ยอดเลขไทย> บาท (<ยอดคำไทย>)...` หลังข้อความเลือกราคาต่ำสุด และไม่พิมพ์คำนำ `ราคากลางจัดซื้อพัสดุ` ซ้ำในสรุป
- Python unit test 10 รายการและ frontend build ผ่าน; ทดสอบผ่าน Chrome จริงว่ากรอกเฉพาะเรื่องต่อท้ายแล้ว Word ที่ดาวน์โหลดมีหัวเรื่องรวมและสรุปตามตัวอย่าง ไม่มี console error; เรนเดอร์ PDF แล้วตรวจการจัดหน้ากับแม่แบบสายช่างโยธา
- ทดสอบเอกสาร 70 รายการหลังปรับ Thai Distributed พบย่อหน้าว่างจากต้นฉบับทำให้ PDF มีหน้าว่างท้ายหนึ่งหน้า แก้โดยตัดเฉพาะย่อหน้าว่างหลังลายเซ็น ทดสอบใหม่ได้ 32 หน้าและหน้าสุดท้ายมีสรุป/ลายเซ็นครบ
- ปรับชื่อไฟล์แคช Word/PDF ให้รวม revision ของ engine เพื่อให้การส่งออกเอกสารเก่าหลังอัปเดตใช้รูปแบบใหม่
- ทดสอบหน้าเว็บทั้ง desktop และมือถือ 390 px: ช่องเรื่องแยกข้อความนำคงที่กับข้อความต่อท้าย ไม่ล้นขอบ; ส่งออกจาก API ได้ Word/PDF ใหม่ใน `demo/` และชุดโปรแกรม macOS ARM สร้างใหม่/เปิดตรวจ health ผ่าน

## Version 1.1 — 17 ก.ย. 2026

- ตรวจไฟล์ `ต้นฉบับ สายช่างโยธา 70.docx` และ `ต้นฉบับ สายพลาธิการ 69.docx` แล้ว พบโครงสร้างแม่แบบแบบสั้น 22 และ 19 ย่อหน้า ต่างจากต้นฉบับแบบ 70 รายการ
- เพิ่มแกน Word ให้ใช้ส่วนหัว เนื้อหาคงที่ และช่องลงชื่อของแม่แบบแบบสั้นที่นำเข้า พร้อมแก้ปัญหาช่องไฟภาษาไทยในย่อหน้ารายการ
- เพิ่ม API นำเข้า `.docx`, ลบแม่แบบแบบคงเอกสารเก่าไว้ และแสดงชื่อแม่แบบแยกในรายการเอกสาร
- ปรับวันที่เป็นข้อความ ปรับข้อมูลซื้อครั้งก่อนเป็นช่องเลขที่พร้อมวันที่เดียว ตัดคำว่า `ทอ.๗๔ ที่` ออกจากผลลัพธ์ และกรองช่องจำนวน/ราคาให้รับตัวเลข
- เพิ่มโลโก้เอกสารและ favicon แบบเดียวกัน พร้อมข้อความ Version 1.1 และผู้พัฒนาในแถบหัว
- `npm run build`, Python unit test 9 รายการ และทดสอบหน้าเว็บด้วย Chrome/Playwright ผ่าน: อัปโหลดทั้ง 2 แม่แบบ เลือกแม่แบบ สร้างเอกสาร กรองช่องตัวเลข บันทึก ดาวน์โหลด Word/PDF ลบแม่แบบ และเปิดเอกสารที่ใช้แม่แบบที่ลบแล้ว ไม่มี console error; หน้ามือถือกว้าง 390 px ไม่ล้นขอบ
- ตรวจ PDF ที่สร้างจากแม่แบบทั้งสองด้วยภาพแล้ว ตราครุฑ หัวเอกสาร รายการ และจุดเซ็นยังอยู่ Word ที่ดาวน์โหลดจริงไม่มี `ทอ.๗๔ ที่` ก่อนข้อความที่กรอก
- สร้างชุด macOS ARM ด้วย PyInstaller สำเร็จและเปิดตรวจ `GET /api/health` ได้; ทดสอบ API แบบครบวงจรกับชุดโปรแกรม: 3 ประเภทสินค้า ยอด 630.00 บาท Word/PDF คัดลอก ลบ/กู้คืน และสำรองข้อมูลผ่าน
- ชุดโปรแกรมล่าสุดอยู่ที่ `dist/QuotationLocal` (macOS ARM) มี `README.md` และไฟล์แม่แบบตัวอย่าง 2 ไฟล์ใน `examples/` สำหรับนำเข้าผ่านหน้าเว็บ
- เก็บไฟล์ที่ดาวน์โหลดจากการทดสอบผ่านหน้าเว็บจริงไว้ที่ `demo/example-v1.1.docx`, `demo/example-v1.1.pdf` และภาพหน้าเอกสาร/หน้ากรอกข้อมูลไว้ใน `demo/`
- เพิ่ม `README.md` ภาษาไทยสำหรับติดตั้ง เปิดใช้ สำรอง/ย้ายข้อมูล และข้อจำกัด; Windows ยังไม่ได้ทดสอบบนเครื่อง Windows จริง

## สถานะล่าสุด

- อัปโหลดโปรเจกต์ไปยัง GitHub เมื่อ 21 ก.ย. 2026: สร้าง Git repository, กันข้อมูล SQLite ในเครื่อง ชุด build และตัวติดตั้งขนาดใหญ่ออกจากประวัติ พร้อมคง `frontend/dist` เพื่อให้ไฟล์เริ่มระบบทำงานได้หลังดาวน์โหลด; frontend build สำเร็จและ Python unit test ผ่าน 10 รายการ; เพิ่ม GitHub Release พร้อม ZIP ชื่อภาษาอังกฤษสำหรับ Windows
- ตรวจ `CODEX_BUILD_PROMPT.md`, Chart, PDF แม่แบบและ `.docx` ต้นฉบับแล้ว
- ต้นฉบับ Word มี 51 หน้า 686 ย่อหน้า ไม่มีตาราง และมีรูปตราครุฑในส่วนหัว
- สร้างแนวทางภาพหน้าหลักที่ `design/primary-screen-concept.png` เพื่อกำหนดสี ระยะ และลำดับการกรอก ภาพเป็นแนวทางภาพ ไม่ใช่ข้อมูลตัวอย่างหรือกติกาเอกสาร
- สร้าง engine Word จาก DOCX ต้นฉบับ โดยคงส่วนหัว ตราครุฑ และส่วนลงชื่อ สร้างเลขข้อ รายการ และยอดสรุปตามข้อมูลจริง PDF แปลงจาก Word เดียวกันด้วย LibreOffice
- สร้างเว็บ React ภาษาไทย: เอกสาร ค้นหา กรอกข้อมูล เพิ่ม/เรียง/คัดลอกรายการ แนะนำชื่อบริษัท ตรวจราคา พรีวิว ดาวน์โหลด Word/PDF แม่แบบและเวอร์ชัน สำรอง/กู้คืน
- ข้อมูลเก็บ SQLite ในเครื่อง ตัวอย่างทดสอบ 3 ประเภทส่งออก Word/PDF และตัวอย่าง 70 รายการได้
- ผ่าน unit test 9 รายการและ browser smoke test: สร้าง 2 รายการ คัดลอกชื่อบริษัทโดยไม่คัดลอกราคา บันทึก ค้นหา และตรวจหน้าจอ desktop/mobile
- เพิ่มสคริปต์เปิดใช้งานบน macOS/Windows และสคริปต์สร้างชุดโปรแกรม native; ทดสอบชุด macOS ARM และการสำรอง/กู้คืนแล้ว

## ต้องยืนยันก่อนเปิดใช้จริง

- ถ้าราคาเคยซื้อต่ำกว่าทุกบริษัท ให้ใช้ราคาใด
- ถ้าหลายบริษัทเสนอราคาต่ำสุดเท่ากัน ให้สรุปอย่างไร
- จำนวนในข้อมูลซื้อครั้งก่อนมีไว้ใช้ในข้อความส่วนใดของเอกสารหรือไม่
- ราคาที่กรอกรวม VAT และค่าใช้จ่ายทั้งหมดแล้วหรือไม่
- เครื่องปลายทางต้องมี LibreOffice และฟอนต์ TH SarabunPSK หรือฟอนต์ต้นฉบับที่ตรงกัน เพื่อให้ PDF จัดหน้าใกล้เคียง Word ต้นฉบับ

## เกณฑ์การรายงาน

บันทึกเฉพาะสิ่งที่ทำเสร็จพร้อมหลักฐานทดสอบ และแยกสิ่งที่ยังไม่เสร็จออกจากกัน อย่าถือภาพ UI หรือไฟล์ตัวอย่างเป็นระบบที่ใช้งานแล้ว
