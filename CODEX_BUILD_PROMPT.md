# คำสั่งสร้างระบบออกใบราคากลาง

> ไฟล์นี้เป็นคำสั่งและสเปกสำหรับ Codex ใน VS Code ให้พัฒนาแอปในโฟลเดอร์นี้จนใช้งานได้จริง อ่านทั้งไฟล์ก่อนเริ่มงาน และถือข้อความจากผู้ใช้ในแชตล่าสุดเป็นลำดับความสำคัญสูงกว่าไฟล์นี้

## วิธีเริ่มงาน

เปิดโฟลเดอร์ Project นี้ใน VS Code แล้วส่งข้อความนี้ให้ Codex:

```text
อ่าน CODEX_BUILD_PROMPT.md และไฟล์อ้างอิงทั้งหมดในโฟลเดอร์นี้ จากนั้นพัฒนาระบบออกใบราคากลางตามสเปกจนใช้งานได้จริงบนเครื่อง local ทำงานเป็นช่วงสั้น ๆ ที่ตรวจสอบได้ ลงมือเขียนโค้ด ทดสอบ แก้ปัญหา และอัปเดตความคืบหน้าใน PROJECT_PROGRESS.md ทุกช่วง อย่าหยุดที่แผนหรือ mockup ถ้าประเด็นใดมีผลต่อกติกาคำนวณราคากลางและสเปกยังไม่ตอบ ให้ถามผมเฉพาะประเด็นนั้นโดยแสดงตัวอย่างผลลัพธ์ที่ต้องเลือก ระหว่างรอคำตอบ ให้ทำส่วนอื่นที่ไม่ขึ้นกับคำตอบต่อไป เมื่อทำเสร็จให้สาธิตขั้นตอนตั้งแต่สร้างเอกสารจนดาวน์โหลด Word/PDF และบอกวิธีติดตั้งบน macOS/Windows
```

## เป้าหมายและขอบเขต

สร้างเว็บแอปภาษาไทยแบบ local-first สำหรับผู้ใช้คนเดียวในเครื่องเดียว ระยะนี้ไม่ต้องมีระบบบัญชีผู้ใช้หรือการทำงานพร้อมกันหลายเครื่อง ใช้ได้โดยไม่ต้องเชื่อมอินเทอร์เน็ตหลังติดตั้ง เปิดใช้งานลื่นไหลบน macOS และ Windows และย้ายไปเครื่องอื่นได้ด้วยขั้นตอนติดตั้งที่ชัดเจน ข้อมูลเอกสารและแม่แบบเก็บในเครื่องของผู้ใช้ และมีวิธีสำรอง/กู้คืนที่ทดสอบได้

ผู้ใช้เลือกแม่แบบ กรอกข้อมูล สร้างรายการจัดซื้อ พรีวิว บันทึก และดาวน์โหลด `.docx` หรือ `.pdf` ได้ จัดการเอกสารที่เคยบันทึกได้ และแก้ไขหรือเพิ่มแม่แบบผ่านหน้าเว็บได้ แอปต้องเน้นการใช้งานจริงมากกว่าหน้าเดโม

## ไฟล์อ้างอิงใน Project

1. `Chart.pdf` — ผังหน้าจอและหมายเลขช่องข้อมูลสีแดง
2. `Template.pdf` — ตัวอย่างผลลัพธ์ต้นฉบับ 51 หน้า
3. `Template (mark).pdf` — ตัวอย่างที่มาร์คตำแหน่งข้อมูล
4. `ราคากลางงานจัดซื้อพัสดุ ส.ส.ต.หลักสูตรพลพยาบาล รุ่นที่ 125 จำนวน 70 รายการ จำนวนเงิน 67,000 บาท (69-0110).docx` — ไฟล์ Word ต้นฉบับที่แก้ไขได้ ใช้เป็นฐานหลักในการรักษารูปแบบเอกสาร

ให้ตรวจไฟล์จริงด้วยเครื่องมืออ่าน PDF/DOCX ก่อนออกแบบ schema และระบบสร้างเอกสาร อย่าอาศัยข้อความที่ extract จาก PDF เพียงอย่างเดียว เพราะการถอดข้อความไทยจาก PDF ผิดรูปได้ ให้ยึด `.docx` และภาพหน้าเอกสารประกอบกัน และทำตาราง mapping ระหว่างเลขสีแดงใน Chart กับ `Template (mark).pdf` ลงในเอกสารเทคนิคของโครงการ

## ความหมายของรอยมาร์ค

- สีเขียว: ข้อมูลที่ผู้ใช้กรอกหรือระบบคำนวณแทนค่า เลขสีแดงกำกับเชื่อมกับช่องใน Chart
- สีเหลืองชุดแรก: เนื้อหาสำหรับรายการที่ **เคยสั่งซื้อ**
- สีเหลืองชุดหลัง (ดูรายการที่ 64): เนื้อหาสำหรับรายการที่ **ไม่เคยสั่งซื้อ**
- แม้หน้าสรุปท้ายเอกสารจะไม่ได้มาร์ค ยอดรวม จำนวนรายการ จำนวนผู้เสนอราคา และรายการเลขข้ออ้างอิงต้องสร้างใหม่จากข้อมูลจริงทุกครั้ง
- เนื้อหาคงที่ ตราครุฑ รูปแบบหัวกระดาษ เลขหน้า และจุดลงชื่อ ต้องคงรูปลักษณ์จากต้นฉบับให้มากที่สุด เลขหน้าและตำแหน่งของส่วนที่ไหลตามจำนวนรายการต้องถูกต้อง

## ขั้นตอนใช้งานหลัก

1. หน้าจัดการเอกสาร: แสดงเอกสารล่าสุด ค้นหา เปิดดู พรีวิว แก้ไข คัดลอก ลบ และดาวน์โหลด Word/PDF ได้ การลบต้องมีการยืนยันและวิธีกู้คืนหรือถังขยะที่เหมาะสม
2. สร้างเอกสาร: เลือกแม่แบบก่อน แล้วกรอกข้อมูลหัวเรื่อง ได้แก่ เรื่อง, ที่, วันที่ และข้อมูลเปลี่ยนได้อื่นที่ตรวจพบจากแม่แบบ แสดงข้อมูลคงที่ซึ่งมาจากแม่แบบให้ตรวจทานได้
3. เพิ่มรายการจัดซื้อทีละรายการ: รายละเอียดพัสดุ จำนวน หน่วย ราคาต่อหน่วย และทางเลือกแหล่งราคา ต้องรองรับเพิ่ม ลบ คัดลอก เรียงลำดับรายการ แล้วเลขรายการ/ข้อย่อยทั้งหมดอัปเดตตามลำดับใหม่
4. รายการทั่วไป: เพิ่มบริษัทได้ไม่จำกัดจำนวน บริษัทแต่ละรายมีราคาที่เสนอ **ต่อหน่วย** และระบบคำนวณราคารวมจากจำนวน x ราคาต่อหน่วย ชื่อบริษัทที่เคยบันทึกต้องแนะนำเมื่อพิมพ์ใกล้เคียง โดยผู้ใช้ต้องเลือกยืนยันชื่อที่แนะนำเอง และยังสร้างชื่อใหม่ได้
5. เมื่อเพิ่มรายการที่ 2 เป็นต้นไป ให้คัดลอกรายชื่อบริษัท **ทั้งหมดจากรายการที่ 1** มาให้ครบ **ไม่คัดลอกราคา** ผู้ใช้แก้รายชื่อบริษัทของแต่ละรายการได้ หากรายการเป็นแบบราคาอ้างอิง ให้ไม่บังคับข้อมูลบริษัท
6. บันทึกฉบับร่างและแก้ไขภายหลังได้ มีสัญญาณบันทึกสำเร็จและป้องกันข้อมูลหายขณะกรอก พรีวิวเป็นเอกสารจริงหรือภาพเอกสารจาก engine เดียวกับการส่งออก ไม่ใช้ตัวอย่าง HTML ที่ข้อความต่างจากไฟล์ดาวน์โหลด
7. ดาวน์โหลด Word และ PDF ของสถานะข้อมูลเดียวกัน ชื่อไฟล์สื่อความหมายและไม่ทำให้เอกสารต้นฉบับถูกเขียนทับ

## ประเภทของรายการและกติกาที่ได้รับคำตอบแล้ว

ออกแบบสถานะประเภทข้อมูลอย่างชัดเจน ไม่ผูก logic กับตำแหน่งแถวในแบบฟอร์ม

### A. เคยสั่งซื้อและราคาต่ำสุดที่บริษัทเสนอเท่ากับราคาเดิม

- ช่อง “เคยสั่งซื้อ” เปิดอยู่ เก็บเลขที่เอกสารการซื้อเดิม จำนวน และราคาที่เคยซื้อ (ดู `ข้อที่ต้องยืนยัน` เรื่องวันที่เอกสารและความหมายของจำนวน)
- เก็บราคาเสนอของบริษัทแต่ละราย คำนวณราคาต่ำสุด เมื่อราคาต่ำสุดเท่าราคาเดิม ให้ใช้ข้อความแบบรายการที่ 1 ใน Word ต้นฉบับ
- หน้าสรุปท้ายเอกสารต้องรวมเลขข้อของรายการกลุ่มนี้ในข้อความ “ราคาที่เคยจัดซื้อครั้งหลังสุด...เท่ากับราคาที่ได้จากการสืบราคา ฯ ...” โดยเลขข้อทั้งสองฝั่งต้องตรงกับข้อที่สร้างจริง

### B. มีราคาอ้างอิง

- เพิ่ม checkbox “มีราคาอ้างอิง” ในรายการ กรอกราคาและจำนวนตามที่ผู้ใช้ระบุ ไม่ต้องกรอกบริษัทหรือราคาเสนอ
- ใช้เนื้อหาและลำดับย่อหน้าแบบรายการที่ 6 (`๒.๖`) ใน Word ต้นฉบับ ทั้งส่วนรายการ ส่วนคำอธิบายราคาอ้างอิง และข้อความสรุปท้ายเอกสาร
- รายละเอียดประกาศ พ.ศ.๒๕๖๖ / วันที่ ๑๒ กรกฎาคม พ.ศ.๒๕๖๖ ในตัวอย่างเป็น **เนื้อหาของแม่แบบนี้** ต้องให้แก้ในระบบแม่แบบได้ อย่าฝังตายทั่วระบบหรือสมมติว่าใช้ได้กับแม่แบบอื่น
- ป้องกันการเลือก “มีราคาอ้างอิง” พร้อมกับแบบที่ขัดกันในรายการเดียว แสดงข้อความอธิบายที่เข้าใจง่าย

### C. ราคาเสนอของบริษัทต่ำกว่าราคาที่เคยซื้อ หรือไม่มีราคาเคยซื้อ

- ใช้ราคาต่ำสุดจากบริษัทที่เสนอราคาครบถ้วนเป็นราคากลางของรายการ และใช้รูปแบบสรุปตามที่ผู้ใช้ระบุ: “ผู้ยื่นเสนอราคาทั้ง n ราย ... เห็นสมควรใช้ราคาต่ำสุดที่ได้จากการสืบราคา ฯ”
- ถ้าไม่เคยซื้อ ใช้เนื้อหาสีเหลืองชุดหลัง แบบรายการที่ 64 (`๒.๖๔`) ไม่มีข้อราคาเคยซื้อ และเลขข้อย่อยของบริษัทต้องเริ่มตามตัวอย่างนั้น
- ถ้าเคยซื้อแต่ราคาเสนอถูกกว่า ให้ใช้เนื้อหารายการและการอ้างเลขข้อให้เหมาะกับแบบที่มีราคาเคยซื้อ แต่สรุปว่าเลือกต่ำสุดจากการสืบราคา
- รายการกลุ่มนี้ต้องแสดงในช่วงเลขข้อที่เกี่ยวข้องในหน้าสรุปท้ายเอกสารอย่างถูกต้อง

**การเงิน:** เก็บราคาเป็นชนิด decimal/หน่วยสตางค์ที่คำนวณได้แน่นอน ห้ามใช้ floating point ตรง ๆ สำหรับผลเงิน คำนวณ `ราคารวมรายการ = จำนวน × ราคาต่อหน่วยที่เลือก` และ `ยอดรวมเอกสาร = ผลรวมราคารวมรายการ` รองรับการแสดงเลขไทย/อารบิกตามจุดในต้นฉบับ ตัวคั่นหลักพัน จำนวนเงินตัวอักษรภาษาไทย และราคารวมภาษีตามข้อความของแม่แบบ แต่ห้ามบวก VAT ซ้ำโดยไม่มีข้อตกลงของผู้ใช้

## การสร้าง Word/PDF และความตรงต้นฉบับ

ให้ทำ proof of concept ของ engine เอกสารก่อนสร้าง UI ทั้งหมด เพราะข้อกำหนดสำคัญที่สุดคือความตรงของเอกสาร Word/PDF กับแม่แบบเดิม

1. สำรวจ styles, sections, header/footer, ระยะขอบ, ฟอนต์, รูปตราครุฑ, tab stop, เลขหน้า และย่อหน้าใน `.docx` ต้นฉบับ ทำแผนว่าจะแยกส่วนคงที่กับส่วนรายการที่ทำซ้ำอย่างไร
2. ใช้ `.docx` ต้นฉบับเป็นฐานหรือเป็นแม่แบบรุ่นแรก รักษารูปแบบของส่วนคงที่โดยแก้เฉพาะจุดแทนค่าและส่วนรายการ หลีกเลี่ยงการสร้างเอกสารใหม่ทั้งฉบับจาก PDF หรือการวาดตัวอักษรทับภาพหน้า PDF
3. สร้างรายการจำนวน 1, 2, และ 70 รายการได้อย่างถูกต้อง เลขข้อ `๒.n`, ข้อย่อยบริษัท, จำนวนราย, การอ้างอิงช่วงข้อในหน้าสรุป, เลขหน้า และการตัดหน้า ต้องไม่ติดค่าตัวอย่าง 70 รายการหรือ 67,000 บาท
4. Word ที่ส่งออกต้องเปิดแก้ไขข้อความได้จริง PDF ต้องสร้างจาก Word ที่ส่งออกหรือจากข้อมูลและ engine เดียวกันด้วยเส้นทางที่ให้ผลตรงกัน อธิบายเครื่องมือแปลง PDF ที่ต้องใช้บน macOS และ Windows และบรรจุ/ติดตั้งให้ใช้งานง่ายที่สุดตามข้อจำกัดสิทธิ์ของเครื่อง
5. เทียบภาพหน้าจากต้นฉบับกับไฟล์ที่สร้างจากข้อมูลตัวอย่าง ทั้งหน้าแรก หน้ากลางที่รายการไหลข้ามหน้า และหน้าสรุปท้าย ตรวจตราครุฑ ตำแหน่งลายเซ็น ระยะห่าง ฟอนต์ ตัวเลข และเนื้อหา ระบุความคลาดเคลื่อนที่ยังแก้ไม่ได้ตามจริง ไม่อ้างว่าเหมือน 100% หากยังไม่ได้ตรวจ
6. เก็บฉบับของแม่แบบที่ใช้สร้างเอกสารไว้กับ metadata ของเอกสาร เพื่อให้เอกสารเก่า preview/download ซ้ำได้ด้วยแม่แบบเดิมแม้ผู้ใช้แก้แม่แบบภายหลัง

## ระบบแก้ไขและเพิ่มแม่แบบผ่านหน้าเว็บ

เสนอให้ทำ **ตัวแก้แม่แบบแบบแยกบล็อกที่ควบคุมได้** แทนการให้ลากข้อความอิสระบนกระดาษ เพราะต้องรักษา Word ให้ใกล้ต้นฉบับและรองรับรายการจำนวนไม่แน่นอน

- ผู้ใช้แก้ข้อความคงที่ส่วนหัว คำนำ ย่อหน้าของแต่ละประเภท รายการคำลงท้าย และชื่อ/ตำแหน่งผู้ลงชื่อได้ผ่านหน้าเว็บ มี placeholder ให้เลือกจากรายการ เช่น `{{subject}}`, `{{date}}`, `{{item.name}}`, `{{item.quantity}}`, `{{quote.company}}`, `{{quote.unit_price}}`, `{{document.total}}`
- บล็อกส่วนรายการและหน้าสรุปมีเงื่อนไขตามประเภท A/B/C แสดงตัวอย่างข้อมูลและพรีวิว Word/PDF ทุกครั้งก่อนเปิดใช้งานแม่แบบใหม่
- เพิ่มแม่แบบโดยคัดลอกจากแม่แบบเดิม หรืออัปโหลด `.docx` แล้วตั้งค่า mapping ช่องข้อมูล/บล็อกผ่านหน้าเว็บ ขั้นแรกอาจต้องให้ผู้ใช้เลือกย่อหน้า/ตำแหน่งที่จะแทนค่าเอง; ห้ามอ้างว่านำเข้า DOCX ใด ๆ แล้วจับช่องอัตโนมัติได้ถูกต้องเสมอ
- เก็บ version ทุกครั้งที่เผยแพร่แม่แบบ แก้ได้บนร่างก่อนใช้งานจริง ย้อนกลับรุ่นเดิมได้ ไม่ให้การแก้รุ่นปัจจุบันเปลี่ยนเอกสารที่สร้างแล้ว
- กำหนดขอบเขตตัวแก้ไขให้ชัด: เนื้อหา placeholder และบล็อกเงื่อนไขแก้ได้ผ่านเว็บ; การจัดหน้า DOCX ขั้นสูง เช่น แก้รูปทรงอิสระ ต้องมีทางอัปโหลดต้นฉบับใหม่และพรีวิว ไม่ต้องทำ Word editor เต็มรูปแบบโดยไร้การควบคุม

## ข้อมูลและพฤติกรรมระบบ

- ออกแบบ schema สำหรับ templates, template_versions, documents, document_items, prior_purchases, vendors, item_quotes, exports หรือข้อมูลเทียบเท่า พร้อม migration และ foreign keys
- ใช้ฐานข้อมูลในเครื่อง เช่น SQLite และบันทึกไฟล์แม่แบบ/ไฟล์ส่งออกในที่เก็บแยกชัดเจน หลีกเลี่ยงเก็บข้อมูลสำคัญไว้ใน browser localStorage เพียงแห่งเดียว
- ข้อมูลบริษัทมีชื่อแสดงจริงและชื่อ normalized สำหรับค้นหาใกล้เคียง รองรับภาษาไทย/อังกฤษ ช่องว่าง จุด คำขึ้นต้น “บริษัท/ร้าน” แนะนำชื่อเดิมแต่ไม่รวมบริษัทโดยอัตโนมัติ เพราะชื่อใกล้กันอาจเป็นคนละนิติบุคคล
- ตรวจกรอกข้อมูล: จำนวน > 0, ราคา >= 0 ตามกติกาที่ตกลง, หน่วยไม่ว่าง, ราคาบริษัทต่อรายการไม่ควรถูกคัดลอกไปอีกแถว, ช่องบังคับตามประเภทครบก่อน export พร้อมข้อความผิดพลาดที่ชี้ไปยังรายการและช่องนั้น
- พรีวิวและไฟล์ส่งออกใช้ข้อมูล revision เดียวกัน มีเวลาแก้ไขและแม่แบบเวอร์ชันชัดเจน
- หน้าเอกสารจัดการค้นหาตามเรื่อง เลขที่ วันที่ บริษัท และสถานะฉบับร่าง/สมบูรณ์ได้ตามข้อมูลที่มี ปุ่มคัดลอกสร้างเอกสารใหม่โดยไม่เขียนทับของเดิม
- สำรองข้อมูลและแม่แบบเป็นไฟล์ชุดเดียว และนำเข้าคืนได้บนอีกเครื่อง รองรับการย้าย Mac ↔ Windows โดยไม่เสียความสัมพันธ์ของไฟล์
- ข้อมูลทั้งหมดอยู่ local ไม่ส่งออกเครือข่ายโดยไม่จำเป็น การเปิด local server ให้ bind เฉพาะ localhost เป็นค่าเริ่มต้น

## หน้าตาและ UX

ใช้ภาษาไทยที่อ่านง่าย สีสบายตา คอนทราสต์เพียงพอและฟอนต์ไทยที่ใช้งานได้ทั้ง Mac/Windows ออกแบบเป็นงานเอกสารราชการที่ร่วมสมัย ไม่ใช้สีหรือ animation มากเกินจำเป็น เน้นฟอร์มที่กรอก 70 รายการได้เร็ว:

- ขั้นตอนหัวเรื่อง → รายการ → ตรวจทาน → พรีวิว/ดาวน์โหลด มีแถบแสดงความคืบหน้าและสถานะบันทึก
- รายการแบบยุบ/ขยายได้ พร้อมสรุปจำนวน บริษัทที่กรอกครบ และราคาที่ระบบจะใช้; ปุ่มเพิ่มรายการอยู่ในตำแหน่งหาเจอและคัดลอกรายชื่อบริษัททันที
- คีย์บอร์ดใช้ได้ดี tab order ถูกต้อง ค่าเงินจัดรูปแบบเมื่อออกจากช่องโดยไม่ทำให้พิมพ์ยาก แจ้งข้อผิดพลาดในจุดที่แก้ได้
- รองรับหน้าจอ laptop และขนาดเล็กโดยยังกรอกรายการได้สะดวก พรีวิวซูมและเปลี่ยนหน้าได้
- อย่าให้หน้าตาใน Chart เป็นข้อบังคับด้านสีหรือ layout; ใช้ Chart เพื่อเข้าใจ flow และ field mapping แล้วออกแบบ UI ใหม่ให้เหมาะใช้งานจริง

## ลำดับการพัฒนาและการตรวจรับ

1. อ่านไฟล์อ้างอิงและบันทึก `DOCUMENT_MAPPING.md`: field สีเขียว, เงื่อนไขสีเหลือง, โครงสร้างข้อย่อย, เนื้อหาคงที่, ฟอนต์/หน้า/ตราครุฑ, กรณี A/B/C และหน้าสรุป
2. สร้าง proof of concept ส่งออก Word/PDF จากข้อมูลตัวอย่าง 3 ประเภท ตรวจความตรงบน Mac/Windows เท่าที่สภาพแวดล้อมอนุญาต แล้วเลือก tech stack และการบรรจุโปรแกรมจากผลทดลองจริง
3. ทำฐานข้อมูลและ logic คำนวณเป็นฟังก์ชันแยกจาก UI/แม่แบบ พร้อมชุดทดสอบที่มีค่าคาดหมายชัดเจน
4. ทำ UI สร้างเอกสาร, จัดการเอกสาร, บริษัทแนะนำ และ editor แม่แบบ จากนั้นต่อกับ engine จริง
5. ทดสอบ end-to-end: สร้างเอกสาร 1/2/70 รายการ; มีและไม่มีราคาเดิม; ราคาอ้างอิง; เพิ่มบริษัท 4 ราย; ชื่อบริษัทคล้ายกัน; คัดลอกรายชื่อแต่ราคาเป็นค่าว่าง; เรียงรายการใหม่; บันทึก/เปิดแก้/คัดลอก/ลบ; แก้แม่แบบแล้วเอกสารเก่ายังใช้เวอร์ชันเดิม; export Word/PDF และกู้คืน backup
6. ตรวจข้อความและเลขอ้างอิงในหน้าสรุปเทียบกับข้อมูลจริงทุกกรณี ตรวจยอดรวมและจำนวนเงินตัวอักษรด้วยตัวอย่างที่รวมสตางค์และมีหลายรายการ ตรวจการตัดหน้าและลายเซ็นเมื่อจำนวนหน้าเปลี่ยน
7. จัดทำ `README.md` ภาษาไทยสำหรับติดตั้ง เปิดแอป ใช้งาน สำรอง/ย้ายข้อมูล และแก้ปัญหา PDF converter บน Mac/Windows พร้อมสคริปต์ build/package ที่รันซ้ำได้

**นิยามเสร็จ:** ผู้ใช้ใหม่ติดตั้งและเปิดแอปได้ตาม README โดยไม่ต้องแก้โค้ด สร้างเอกสารตัวอย่างจริง พรีวิวตรงไฟล์ที่ดาวน์โหลด Word แก้ไขได้ PDF อ่านได้ ข้อมูลบันทึกอยู่หลังปิดเปิดแอป รายการและยอด/เลขข้อถูกต้อง และจัดการแม่แบบกับเอกสารได้ตามขอบเขตนี้ อย่าประกาศว่าเสร็จจนกว่าจะมีหลักฐานทดสอบและระบุข้อจำกัดที่เหลือ

## ข้อที่ต้องยืนยันกับผู้ใช้ก่อนสรุปกติกาหรือเปิดใช้จริง

คำตอบเหล่านี้ยังไม่ได้รับครบ ให้ Codex ทำส่วนอื่นต่อได้ แต่ห้ามเดากติกาที่ทำให้ราคากลางผิด:

1. เมื่อราคาเคยซื้อ **ต่ำกว่า** ราคาเสนอทุกบริษัท ต้องใช้ราคาใดเป็นราคากลางและเขียนสรุปแบบใด?
2. ถ้าบริษัทไม่เสนอราคาในบางรายการ หรือมีบริษัทเสนอราคาต่ำสุดเท่ากันหลายราย ให้แสดงจำนวนผู้เสนอและสรุปอย่างไร? ต้องมีขั้นต่ำกี่บริษัทหรือไม่?
3. “จำนวน” ในข้อมูลซื้อครั้งก่อนหมายถึงจำนวนที่ซื้อครั้งก่อน หรือจำนวนที่จะซื้อครั้งนี้? หากต่างกัน ต้องแสดงจำนวนใดในเอกสาร?
4. บรรทัดราคาเคยซื้อในต้นฉบับมี **วันที่เอกสาร** นอกจากเลขที่เอกสาร ต้องให้ผู้ใช้กรอกวันที่ด้วยหรือใช้ข้อความจากแม่แบบ?
5. ราคาที่ผู้ใช้กรอกเป็นราคารวม VAT และค่าใช้จ่ายทั้งหมดแล้วใช่หรือไม่? หากไม่ใช่ ต้องคำนวณหรือแสดงแยกอย่างไร?
6. หากข้อมูลในหัวเอกสารหรือกรรมการเปลี่ยนตามรอบงาน ต้องให้กรอกในแต่ละเอกสาร หรือแก้ในแม่แบบรุ่นใหม่?

ถ้ายังไม่ได้รับคำตอบ ให้บันทึกสมมติฐานและสถานะที่รอคำตอบใน `PROJECT_PROGRESS.md` และห้ามซ่อนกรณีที่ระบบยังตัดสินราคากลางไม่ได้โดยอัตโนมัติ

## วิธีทำงานของ Codex

- ลงมือทำงานต่อเนื่องตามเป้าหมาย ไม่หยุดหลังเขียนแผนหรือ mockup หากทำเป็นหลายรอบ ให้ทุกครั้งอ่าน `PROJECT_PROGRESS.md` และตรวจสถานะโค้ดก่อนทำต่อ
- แยกการตัดสินใจเรื่องเอกสารและราคาที่มีผลจริงออกจากการตัดสินใจเทคนิคทั่วไป เรื่องเทคนิคให้เลือกและทดลองเอง เรื่องราคาที่ไม่ชัดให้ถามพร้อมตัวอย่าง
- อัปเดต `PROJECT_PROGRESS.md` ว่าทำอะไรแล้ว ทดสอบอะไรผ่าน/ไม่ผ่าน เหลืออะไร และคำสั่งต่อไปที่ชัดเจน ไม่รายงานว่าเสร็จหากยังเป็น prototype
- เมื่อพบความคลาดเคลื่อนระหว่าง `.docx`, PDF และรอยมาร์ค ให้แสดงหน้า/ย่อหน้าที่ขัดกันและยึดคำตอบผู้ใช้เป็นหลัก
- ห้ามแก้หรือเขียนทับไฟล์อ้างอิงต้นฉบับทั้งสี่ไฟล์
